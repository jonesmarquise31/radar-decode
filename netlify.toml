[build]
  publish = "."
  functions = "netlify/functions"

# Clean URL: /thanks serves thanks.html (rewrite, not redirect — URL stays /thanks)
[[redirects]]
  from = "/thanks"
  to = "/thanks.html"
  status = 200
